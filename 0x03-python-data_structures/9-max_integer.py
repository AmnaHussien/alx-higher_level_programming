#!/usr/bin/python3

/**
  * max_integer - function that finds the biggest integer of alist
  * my_list: lists
  * Return: biggest integer in the lists
  *
 */
def max_integer(my_list=[]):
    for i in range(len(my_list=[])):
        if my_list[i] < my_list[i+1]:
            continue
        else:
            print(i)
        i = i +1
