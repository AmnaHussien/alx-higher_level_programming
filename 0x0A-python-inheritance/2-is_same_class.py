#!/usr/bin/python3
"""
if same or not
"""


def is_same_class(obj, a_class):
    """ determine if same class or not """
    if type(obj) == a_class:
        return (True)
    else:
        return(False)
